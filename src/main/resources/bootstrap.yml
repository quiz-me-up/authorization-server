spring:
  application:
    name: authorization-server-application

---
spring:
  config:
    activate:
      on-profile: "!kubernetes"
  cloud:
    config:
      enabled: true
      fail-fast: true
      name: ${spring.application.name}, mongo
      uri: http://localhost:1000/configuration